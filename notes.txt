mariadb -h mariadb-server -P 3306 -u root -pLufi666Lufi --ssl=0

mariadb -h mariadb-maxscale -P 4000 -u lufi -plufilufi --ssl=0 LUFI

mariadb -h mariadb-maxscale -P 4443 -u lufi -plufilufi LUFI

tail -f /var/log/maxscale/maxscale.log

less /var/log/maxscale/maxscale.log

SHOW GRANTS FOR maxscale_monitor;

SHOW VARIABLES LIKE 'general_log%';
SET GLOBAL general_log = 'ON';
SET GLOBAL general_log = 'OFF';

SET GLOBAL general_log_file = '/var/log/mysql/mariadb-query.log';

INSTALL SONAME 'server_audit';
